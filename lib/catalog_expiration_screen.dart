import 'package:flutter/material.dart';

class CatalogExpirationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Catalog Expiration Screen'),
      ),
      body: Center(
        child: Text('Catalog Expiration Details'),
      ),
    );
  }
}
